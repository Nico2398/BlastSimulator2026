{
  "name": "level3-playthrough-ecology",
  "description": "Level 3 ecological shutdown scenario: massive krackle and obliviax blasts with no mitigation, long tick cycles to trigger ecology collapse",
  "steps": [
    {
      "command": "new_game seed:3666",
      "interaction": [
        {
          "type": "command",
          "command": "new_game seed:3666"
        }
      ],
      "role": "setup"
    },
    {
      "command": "campaign start level:treranium_depths",
      "interaction": [
        {
          "type": "command",
          "command": "campaign start level:treranium_depths"
        }
      ],
      "role": "setup"
    },
    {
      "command": "time resume",
      "interaction": [
        {
          "type": "command",
          "command": "time resume"
        }
      ],
      "role": "setup"
    },
    {
      "command": "scores",
      "interaction": [
        {
          "type": "command",
          "command": "scores"
        }
      ],
      "role": "observe"
    },
    {
      "command": "finances",
      "interaction": [
        {
          "type": "command",
          "command": "finances"
        }
      ],
      "role": "observe"
    },
    {
      "command": "employee hire role:driller",
      "interaction": [
        {
          "type": "clickSelector",
          "selector": "#bs-toolbar [data-panel=\"employees\"]"
        },
        {
          "type": "waitForSelector",
          "selector": "#bs-employee-panel [data-role=\"driller\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-employee-panel [data-role=\"driller\"]"
        }
      ],
      "role": "player"
    },
    {
      "command": "employee hire role:driller",
      "interaction": [
        {
          "type": "waitForSelector",
          "selector": "#bs-employee-panel [data-role=\"driller\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-employee-panel [data-role=\"driller\"]"
        }
      ],
      "role": "player"
    },
    {
      "command": "employee hire role:driller",
      "interaction": [
        {
          "type": "waitForSelector",
          "selector": "#bs-employee-panel [data-role=\"driller\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-employee-panel [data-role=\"driller\"]"
        }
      ],
      "role": "player"
    },
    {
      "command": "employee hire role:blaster",
      "interaction": [
        {
          "type": "waitForSelector",
          "selector": "#bs-employee-panel [data-role=\"blaster\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-employee-panel [data-role=\"blaster\"]"
        }
      ],
      "role": "player"
    },
    {
      "command": "employee hire role:blaster",
      "interaction": [
        {
          "type": "waitForSelector",
          "selector": "#bs-employee-panel [data-role=\"blaster\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-employee-panel [data-role=\"blaster\"]"
        }
      ],
      "role": "player"
    },
    {
      "command": "employee hire role:blaster",
      "interaction": [
        {
          "type": "waitForSelector",
          "selector": "#bs-employee-panel [data-role=\"blaster\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-employee-panel [data-role=\"blaster\"]"
        }
      ],
      "role": "player"
    },
    {
      "command": "employee hire role:driver",
      "interaction": [
        {
          "type": "waitForSelector",
          "selector": "#bs-employee-panel [data-role=\"driver\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-employee-panel [data-role=\"driver\"]"
        }
      ],
      "role": "player"
    },
    {
      "command": "employee list",
      "interaction": [
        {
          "type": "command",
          "command": "employee list"
        }
      ],
      "role": "observe"
    },
    {
      "command": "state full",
      "interaction": [
        {
          "type": "command",
          "command": "state full"
        }
      ],
      "role": "observe"
    },
    {
      "command": "tick 5",
      "interaction": [
        {
          "type": "command",
          "command": "tick 5"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 5",
      "interaction": [
        {
          "type": "command",
          "command": "tick 5"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "scores",
      "interaction": [
        {
          "type": "command",
          "command": "scores"
        }
      ],
      "role": "observe"
    },
    {
      "command": "drill_plan grid rows:5 cols:4 spacing:3 depth:12 start:10,10",
      "interaction": [
        {
          "type": "clickSelector",
          "selector": "#bs-toolbar [data-panel=\"blast\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-blast-panel [data-step=\"1\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-blast-panel [data-action=\"grid-tool\"]"
        },
        {
          "type": "waitForSelector",
          "selector": "#bs-tile-select-confirm"
        },
        {
          "type": "dragTiles",
          "x1": 10,
          "z1": 10,
          "x2": 19,
          "z2": 22
        },
        {
          "type": "screenshot"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-tile-select-confirm"
        }
      ],
      "role": "player"
    },
    {
      "command": "charge hole:* explosive:krackle amount:10 stemming:1",
      "interaction": [
        {
          "type": "command",
          "command": "charge hole:* explosive:krackle amount:10 stemming:1"
        }
      ],
      "description": "Left unmarked: this explosive/amount/stemming doesn't match the Charge panel's defaults (boomite/5kg/2m), and there is no proven click path to an exact non-default amount -- Finding #10's class, Charge All is the only commit at whatever the steppers currently show."
    },
    {
      "command": "sequence auto delay_step:10",
      "interaction": [
        {
          "type": "command",
          "command": "sequence auto delay_step:10"
        }
      ],
      "description": "Left unmarked: this delay_step doesn't match the Sequence panel's default (25ms), and the stepper that changes it has never been click-tested in this conversion (Batch 6's safety-projection-visual note) -- left as a command rather than guessing at an unproven selector."
    },
    {
      "command": "blast",
      "timeout": 180,
      "interaction": [
        {
          "type": "command",
          "command": "blast"
        },
        {
          "type": "waitForSelector",
          "selector": "[data-action=\"report-close\"]"
        },
        {
          "type": "clickSelector",
          "selector": "[data-action=\"report-close\"]"
        }
      ],
      "description": "campaign start level:treranium_depths FAILS at step 1 (\"Level treranium_depths is locked. Complete previous levels first.\" -- Campaign.ts unlocks only tier-1 levels by default, treranium_depths is tier 3) -- a real, pre-existing scenario bug, not introduced by this conversion. Every subsequent command runs against whatever bare new_game seed:3666 produced instead of the intended level, and by this cycle the mismatch leaves the Fire step's execute button disabled -- validateBlastPlan genuinely rejects this plan on the wrong world, not a selector or tab-visibility issue. The detonate itself stays a command rather than force a click that cannot land, but the resulting BlastReportModal opens regardless of how the blast fired (Finding #12 -- state-reactive, not click-reactive) and blocks every later panel click until closed, so the close IS a real click here. See Finding #24 in the plan doc."
    },
    {
      "command": "state full",
      "timeout": 180,
      "interaction": [
        {
          "type": "command",
          "command": "state full"
        }
      ],
      "role": "observe"
    },
    {
      "command": "scores",
      "interaction": [
        {
          "type": "command",
          "command": "scores"
        }
      ],
      "role": "observe"
    },
    {
      "command": "drill_plan grid rows:4 cols:5 spacing:3 depth:12 start:25,10",
      "interaction": [
        {
          "type": "clickSelector",
          "selector": "#bs-blast-panel [data-step=\"1\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-blast-panel [data-action=\"grid-tool\"]"
        },
        {
          "type": "waitForSelector",
          "selector": "#bs-tile-select-confirm"
        },
        {
          "type": "dragTiles",
          "x1": 25,
          "z1": 10,
          "x2": 37,
          "z2": 19
        },
        {
          "type": "screenshot"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-tile-select-confirm"
        }
      ],
      "role": "player"
    },
    {
      "command": "charge hole:* explosive:krackle amount:10 stemming:1",
      "interaction": [
        {
          "type": "command",
          "command": "charge hole:* explosive:krackle amount:10 stemming:1"
        }
      ],
      "description": "Left unmarked: this explosive/amount/stemming doesn't match the Charge panel's defaults (boomite/5kg/2m), and there is no proven click path to an exact non-default amount -- Finding #10's class, Charge All is the only commit at whatever the steppers currently show."
    },
    {
      "command": "sequence auto delay_step:10",
      "interaction": [
        {
          "type": "command",
          "command": "sequence auto delay_step:10"
        }
      ],
      "description": "Left unmarked: this delay_step doesn't match the Sequence panel's default (25ms), and the stepper that changes it has never been click-tested in this conversion (Batch 6's safety-projection-visual note) -- left as a command rather than guessing at an unproven selector."
    },
    {
      "command": "blast",
      "timeout": 180,
      "interaction": [
        {
          "type": "command",
          "command": "blast"
        },
        {
          "type": "waitForSelector",
          "selector": "[data-action=\"report-close\"]"
        },
        {
          "type": "clickSelector",
          "selector": "[data-action=\"report-close\"]"
        }
      ],
      "description": "campaign start level:treranium_depths FAILS at step 1 (\"Level treranium_depths is locked. Complete previous levels first.\" -- Campaign.ts unlocks only tier-1 levels by default, treranium_depths is tier 3) -- a real, pre-existing scenario bug, not introduced by this conversion. Every subsequent command runs against whatever bare new_game seed:3666 produced instead of the intended level, and by this cycle the mismatch leaves the Fire step's execute button disabled -- validateBlastPlan genuinely rejects this plan on the wrong world, not a selector or tab-visibility issue. The detonate itself stays a command rather than force a click that cannot land, but the resulting BlastReportModal opens regardless of how the blast fired (Finding #12 -- state-reactive, not click-reactive) and blocks every later panel click until closed, so the close IS a real click here. See Finding #24 in the plan doc."
    },
    {
      "command": "state full",
      "timeout": 180,
      "interaction": [
        {
          "type": "command",
          "command": "state full"
        }
      ],
      "role": "observe"
    },
    {
      "command": "scores",
      "interaction": [
        {
          "type": "command",
          "command": "scores"
        }
      ],
      "role": "observe"
    },
    {
      "command": "tick 8",
      "interaction": [
        {
          "type": "command",
          "command": "tick 8"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 8",
      "interaction": [
        {
          "type": "command",
          "command": "tick 8"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 8",
      "interaction": [
        {
          "type": "command",
          "command": "tick 8"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "scores",
      "interaction": [
        {
          "type": "command",
          "command": "scores"
        }
      ],
      "role": "observe"
    },
    {
      "command": "drill_plan grid rows:5 cols:5 spacing:3 depth:14 start:10,25",
      "interaction": [
        {
          "type": "clickSelector",
          "selector": "#bs-blast-panel [data-step=\"1\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-blast-panel [data-action=\"grid-tool\"]"
        },
        {
          "type": "waitForSelector",
          "selector": "#bs-tile-select-confirm"
        },
        {
          "type": "dragTiles",
          "x1": 10,
          "z1": 25,
          "x2": 22,
          "z2": 37
        },
        {
          "type": "screenshot"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-tile-select-confirm"
        }
      ],
      "role": "player"
    },
    {
      "command": "charge hole:* explosive:obliviax amount:8 stemming:1",
      "interaction": [
        {
          "type": "command",
          "command": "charge hole:* explosive:obliviax amount:8 stemming:1"
        }
      ],
      "description": "Left unmarked: this explosive/amount/stemming doesn't match the Charge panel's defaults (boomite/5kg/2m), and there is no proven click path to an exact non-default amount -- Finding #10's class, Charge All is the only commit at whatever the steppers currently show."
    },
    {
      "command": "sequence auto delay_step:10",
      "interaction": [
        {
          "type": "command",
          "command": "sequence auto delay_step:10"
        }
      ],
      "description": "Left unmarked: this delay_step doesn't match the Sequence panel's default (25ms), and the stepper that changes it has never been click-tested in this conversion (Batch 6's safety-projection-visual note) -- left as a command rather than guessing at an unproven selector."
    },
    {
      "command": "blast",
      "timeout": 180,
      "interaction": [
        {
          "type": "command",
          "command": "blast"
        },
        {
          "type": "waitForSelector",
          "selector": "[data-action=\"report-close\"]"
        },
        {
          "type": "clickSelector",
          "selector": "[data-action=\"report-close\"]"
        }
      ],
      "description": "campaign start level:treranium_depths FAILS at step 1 (\"Level treranium_depths is locked. Complete previous levels first.\" -- Campaign.ts unlocks only tier-1 levels by default, treranium_depths is tier 3) -- a real, pre-existing scenario bug, not introduced by this conversion. Every subsequent command runs against whatever bare new_game seed:3666 produced instead of the intended level, and by this cycle the mismatch leaves the Fire step's execute button disabled -- validateBlastPlan genuinely rejects this plan on the wrong world, not a selector or tab-visibility issue. The detonate itself stays a command rather than force a click that cannot land, but the resulting BlastReportModal opens regardless of how the blast fired (Finding #12 -- state-reactive, not click-reactive) and blocks every later panel click until closed, so the close IS a real click here. See Finding #24 in the plan doc."
    },
    {
      "command": "state full",
      "timeout": 180,
      "interaction": [
        {
          "type": "command",
          "command": "state full"
        }
      ],
      "role": "observe"
    },
    {
      "command": "scores",
      "interaction": [
        {
          "type": "command",
          "command": "scores"
        }
      ],
      "role": "observe"
    },
    {
      "command": "drill_plan grid rows:5 cols:5 spacing:3 depth:14 start:25,25",
      "interaction": [
        {
          "type": "clickSelector",
          "selector": "#bs-blast-panel [data-step=\"1\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-blast-panel [data-action=\"grid-tool\"]"
        },
        {
          "type": "waitForSelector",
          "selector": "#bs-tile-select-confirm"
        },
        {
          "type": "dragTiles",
          "x1": 25,
          "z1": 25,
          "x2": 37,
          "z2": 37
        },
        {
          "type": "screenshot"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-tile-select-confirm"
        }
      ],
      "role": "player"
    },
    {
      "command": "charge hole:* explosive:krackle amount:12 stemming:1",
      "interaction": [
        {
          "type": "command",
          "command": "charge hole:* explosive:krackle amount:12 stemming:1"
        }
      ],
      "description": "Left unmarked: this explosive/amount/stemming doesn't match the Charge panel's defaults (boomite/5kg/2m), and there is no proven click path to an exact non-default amount -- Finding #10's class, Charge All is the only commit at whatever the steppers currently show."
    },
    {
      "command": "sequence auto delay_step:10",
      "interaction": [
        {
          "type": "command",
          "command": "sequence auto delay_step:10"
        }
      ],
      "description": "Left unmarked: this delay_step doesn't match the Sequence panel's default (25ms), and the stepper that changes it has never been click-tested in this conversion (Batch 6's safety-projection-visual note) -- left as a command rather than guessing at an unproven selector."
    },
    {
      "command": "blast",
      "timeout": 180,
      "interaction": [
        {
          "type": "command",
          "command": "blast"
        }
      ],
      "description": "campaign start level:treranium_depths FAILS at step 1 (\"Level treranium_depths is locked. Complete previous levels first.\" -- Campaign.ts unlocks only tier-1 levels by default, treranium_depths is tier 3) -- a real, pre-existing scenario bug, not introduced by this conversion. Every subsequent command runs against whatever bare new_game seed:3666 produced instead of the intended level, and by this cycle the mismatch leaves the Fire step's execute button disabled -- validateBlastPlan genuinely rejects this plan on the wrong world, not a selector or tab-visibility issue. The detonate itself stays a command rather than force a click that cannot land, but the resulting BlastReportModal opens regardless of how the blast fired (Finding #12 -- state-reactive, not click-reactive) and blocks every later panel click until closed, so the close IS a real click here. See Finding #24 in the plan doc. This specific cycle's blast fails outright (\"Invalid plan: Missing charge\" on every hole) rather than just failing validation -- no report is produced, so no modal opens and no close click is needed here."
    },
    {
      "command": "state full",
      "timeout": 180,
      "interaction": [
        {
          "type": "command",
          "command": "state full"
        }
      ],
      "role": "observe"
    },
    {
      "command": "scores",
      "interaction": [
        {
          "type": "command",
          "command": "scores"
        }
      ],
      "role": "observe"
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "scores",
      "interaction": [
        {
          "type": "command",
          "command": "scores"
        }
      ],
      "role": "observe"
    },
    {
      "command": "drill_plan grid rows:6 cols:5 spacing:3 depth:16 start:15,15",
      "interaction": [
        {
          "type": "clickSelector",
          "selector": "#bs-blast-panel [data-step=\"1\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-blast-panel [data-action=\"grid-tool\"]"
        },
        {
          "type": "waitForSelector",
          "selector": "#bs-tile-select-confirm"
        },
        {
          "type": "dragTiles",
          "x1": 15,
          "z1": 15,
          "x2": 27,
          "z2": 30
        },
        {
          "type": "screenshot"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-tile-select-confirm"
        }
      ],
      "role": "player"
    },
    {
      "command": "charge hole:* explosive:obliviax amount:10 stemming:1",
      "interaction": [
        {
          "type": "command",
          "command": "charge hole:* explosive:obliviax amount:10 stemming:1"
        }
      ],
      "description": "Left unmarked: this explosive/amount/stemming doesn't match the Charge panel's defaults (boomite/5kg/2m), and there is no proven click path to an exact non-default amount -- Finding #10's class, Charge All is the only commit at whatever the steppers currently show."
    },
    {
      "command": "sequence auto delay_step:10",
      "interaction": [
        {
          "type": "command",
          "command": "sequence auto delay_step:10"
        }
      ],
      "description": "Left unmarked: this delay_step doesn't match the Sequence panel's default (25ms), and the stepper that changes it has never been click-tested in this conversion (Batch 6's safety-projection-visual note) -- left as a command rather than guessing at an unproven selector."
    },
    {
      "command": "blast",
      "timeout": 180,
      "interaction": [
        {
          "type": "command",
          "command": "blast"
        },
        {
          "type": "waitForSelector",
          "selector": "[data-action=\"report-close\"]"
        },
        {
          "type": "clickSelector",
          "selector": "[data-action=\"report-close\"]"
        }
      ],
      "description": "campaign start level:treranium_depths FAILS at step 1 (\"Level treranium_depths is locked. Complete previous levels first.\" -- Campaign.ts unlocks only tier-1 levels by default, treranium_depths is tier 3) -- a real, pre-existing scenario bug, not introduced by this conversion. Every subsequent command runs against whatever bare new_game seed:3666 produced instead of the intended level, and by this cycle the mismatch leaves the Fire step's execute button disabled -- validateBlastPlan genuinely rejects this plan on the wrong world, not a selector or tab-visibility issue. The detonate itself stays a command rather than force a click that cannot land, but the resulting BlastReportModal opens regardless of how the blast fired (Finding #12 -- state-reactive, not click-reactive) and blocks every later panel click until closed, so the close IS a real click here. See Finding #24 in the plan doc."
    },
    {
      "command": "state full",
      "timeout": 180,
      "interaction": [
        {
          "type": "command",
          "command": "state full"
        }
      ],
      "role": "observe"
    },
    {
      "command": "scores",
      "interaction": [
        {
          "type": "command",
          "command": "scores"
        }
      ],
      "role": "observe"
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "scores",
      "interaction": [
        {
          "type": "command",
          "command": "scores"
        }
      ],
      "role": "observe"
    },
    {
      "command": "drill_plan grid rows:6 cols:6 spacing:3 depth:16 start:20,20",
      "interaction": [
        {
          "type": "clickSelector",
          "selector": "#bs-blast-panel [data-step=\"1\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-blast-panel [data-action=\"grid-tool\"]"
        },
        {
          "type": "waitForSelector",
          "selector": "#bs-tile-select-confirm"
        },
        {
          "type": "dragTiles",
          "x1": 20,
          "z1": 20,
          "x2": 35,
          "z2": 35
        },
        {
          "type": "screenshot"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-tile-select-confirm"
        }
      ],
      "role": "player"
    },
    {
      "command": "charge hole:* explosive:krackle amount:15 stemming:1",
      "interaction": [
        {
          "type": "command",
          "command": "charge hole:* explosive:krackle amount:15 stemming:1"
        }
      ],
      "description": "Left unmarked: this explosive/amount/stemming doesn't match the Charge panel's defaults (boomite/5kg/2m), and there is no proven click path to an exact non-default amount -- Finding #10's class, Charge All is the only commit at whatever the steppers currently show."
    },
    {
      "command": "sequence auto delay_step:10",
      "interaction": [
        {
          "type": "command",
          "command": "sequence auto delay_step:10"
        }
      ],
      "description": "Left unmarked: this delay_step doesn't match the Sequence panel's default (25ms), and the stepper that changes it has never been click-tested in this conversion (Batch 6's safety-projection-visual note) -- left as a command rather than guessing at an unproven selector."
    },
    {
      "command": "blast",
      "timeout": 180,
      "interaction": [
        {
          "type": "command",
          "command": "blast"
        }
      ],
      "description": "campaign start level:treranium_depths FAILS at step 1 (\"Level treranium_depths is locked. Complete previous levels first.\" -- Campaign.ts unlocks only tier-1 levels by default, treranium_depths is tier 3) -- a real, pre-existing scenario bug, not introduced by this conversion. Every subsequent command runs against whatever bare new_game seed:3666 produced instead of the intended level, and by this cycle the mismatch leaves the Fire step's execute button disabled -- validateBlastPlan genuinely rejects this plan on the wrong world, not a selector or tab-visibility issue. The detonate itself stays a command rather than force a click that cannot land, but the resulting BlastReportModal opens regardless of how the blast fired (Finding #12 -- state-reactive, not click-reactive) and blocks every later panel click until closed, so the close IS a real click here. See Finding #24 in the plan doc. This specific cycle's blast fails outright (\"Invalid plan: Missing charge\" on every hole) rather than just failing validation -- no report is produced, so no modal opens and no close click is needed here."
    },
    {
      "command": "state full",
      "timeout": 180,
      "interaction": [
        {
          "type": "command",
          "command": "state full"
        }
      ],
      "role": "observe"
    },
    {
      "command": "scores",
      "interaction": [
        {
          "type": "command",
          "command": "scores"
        }
      ],
      "role": "observe"
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "scores",
      "interaction": [
        {
          "type": "command",
          "command": "scores"
        }
      ],
      "role": "observe"
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "timeout": 90,
      "role": "setup"
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 -- follows a bare tick, so whether an event is pending is a random roll; there may be no dialog on screen. Deliberately left unmarked (Finding #15)."
    },
    {
      "command": "scores",
      "interaction": [
        {
          "type": "command",
          "command": "scores"
        }
      ],
      "role": "observe"
    },
    {
      "command": "finances",
      "interaction": [
        {
          "type": "command",
          "command": "finances"
        }
      ],
      "role": "observe"
    },
    {
      "command": "stats",
      "interaction": [
        {
          "type": "command",
          "command": "stats"
        }
      ],
      "role": "observe"
    },
    {
      "command": "campaign status",
      "interaction": [
        {
          "type": "command",
          "command": "campaign status"
        }
      ],
      "role": "setup"
    }
  ]
}
