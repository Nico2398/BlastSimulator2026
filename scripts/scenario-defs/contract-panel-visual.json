{
  "name": "contract-panel-visual",
  "description": "Visual verification of the contract panel UI: available contracts list with material type, quantity, unit price, deadline and penalties; active contracts with progress; completed contract history; accept/reject/negotiate buttons. Accept, the delivery amount and Deliver are all driven through the Contracts panel's own controls (issue #479); only the clock, world setup and the read-only list/status steps remain console commands The Contracts panel is opened once and left open: the tool rail TOGGLES a panel, so re-clicking its entry would close the panel the next step needs.",
  "steps": [
    {
      "command": "new_game seed:42",
      "timeout": 15,
      "interaction": [
        {
          "type": "command",
          "command": "new_game seed:42"
        }
      ],
      "role": "setup",
      "expect": {
        "equals": {
          "cash": 50000,
          "activeContractCount": 0
        }
      }
    },
    {
      "command": "campaign start level:dusty_hollow",
      "interaction": [
        {
          "type": "command",
          "command": "campaign start level:dusty_hollow"
        }
      ],
      "role": "setup",
      "expect": {
        "equals": {
          "cash": 50000
        }
      }
    },
    {
      "command": "time resume",
      "interaction": [
        {
          "type": "command",
          "command": "time resume"
        }
      ],
      "role": "setup"
    },
    {
      "command": "contract list",
      "interaction": [
        {
          "type": "command",
          "command": "contract list"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-toolbar [data-panel=\"contracts\"]"
        }
      ],
      "role": "observe"
    },
    {
      "command": "contract accept 1",
      "interaction": [
        {
          "type": "awaitUsable",
          "selector": "#bs-contract-panel .bs-contract-accept"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-contract-panel .bs-contract-accept"
        }
      ],
      "role": "player",
      "expect": {
        "equals": {
          "activeContractCount": 1,
          "cash": 50000
        },
        "note": "real click on the Contracts panel's first available row -- contract #1 (Supply dirtite) is genuinely first in both the console list and the panel, matching `contract accept 1`'s explicit id; no Finding #55-class mismatch here"
      }
    },
    {
      "command": "contract status",
      "interaction": [
        {
          "type": "command",
          "command": "contract status"
        }
      ],
      "role": "observe"
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "role": "setup",
      "expect": {
        "equals": {
          "cash": 50000
        }
      }
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 \u2014 follows a bare tick, so whether an event is pending is a random roll and there may be no dialog on screen at all. Deliberately left unconverted: a player step must name a control that is actually there, and forcing a click here would fail on the roll rather than on a defect."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "role": "setup",
      "expect": {
        "equals": {
          "cash": 50000
        }
      }
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 \u2014 follows a bare tick, so whether an event is pending is a random roll and there may be no dialog on screen at all. Deliberately left unconverted: a player step must name a control that is actually there, and forcing a click here would fail on the roll rather than on a defect."
    },
    {
      "command": "contract list",
      "interaction": [
        {
          "type": "command",
          "command": "contract list"
        }
      ],
      "role": "observe"
    },
    {
      "command": "contract accept 2",
      "interaction": [
        {
          "type": "awaitUsable",
          "selector": "#bs-contract-panel .bs-contract-accept"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-contract-panel .bs-contract-accept"
        }
      ],
      "role": "player",
      "expect": {
        "equals": {
          "activeContractCount": 2,
          "cash": 50000
        },
        "note": "2nd real click on the same unqualified .bs-contract-accept selector -- with contract #1 already moved to active, the panel's first available row is now #2 (Dispose of rubble), matching `contract accept 2`'s explicit id; verified via a real interaction-mode run rather than assumed from panel-ordering logic"
      }
    },
    {
      "command": "contract status",
      "interaction": [
        {
          "type": "command",
          "command": "contract status"
        }
      ],
      "role": "observe"
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "role": "setup",
      "expect": {
        "equals": {
          "cash": 50000
        }
      }
    },
    {
      "command": "event choose 0",
      "interaction": [
        {
          "type": "command",
          "command": "event choose 0"
        }
      ],
      "description": "event choose 0 \u2014 follows a bare tick, so whether an event is pending is a random roll and there may be no dialog on screen at all. Deliberately left unconverted: a player step must name a control that is actually there, and forcing a click here would fail on the roll rather than on a defect."
    },
    {
      "command": "contract deliver 2 amount:300",
      "interaction": [
        {
          "type": "command",
          "command": "contract deliver 2 amount:300"
        }
      ],
      "description": "BLOCKED FINDING, left unconverted deliberately: this scenario never mines or hauls any ore, so nothing is in storage and the Contracts panel correctly disables Deliver (maxDeliverable <= 0). The console rejects it too \u2014 consumeStoredOre fails \u2014 but it returns success:false rather than throwing, and the command-mode runner only fails a step on a thrown exception, so command mode reports this no-op as a pass (the #445 class). Converting it to a click is what surfaced that; papering over it with a command would hide it again.",
      "expect": {
        "equals": {
          "activeContractCount": 2,
          "cash": 50000
        },
        "note": "delivery fails honestly (0.0 kg available) -- this scenario never mines or hauls, so nothing is ever in storage; see this step's own BLOCKED FINDING description for the #445 silent-no-op class this surfaced"
      }
    },
    {
      "command": "contract status",
      "interaction": [
        {
          "type": "command",
          "command": "contract status"
        }
      ],
      "role": "observe"
    },
    {
      "command": "finances",
      "interaction": [
        {
          "type": "command",
          "command": "finances"
        }
      ],
      "role": "observe",
      "expect": {
        "equals": {
          "cash": 50000
        }
      }
    },
    {
      "command": "state full",
      "interaction": [
        {
          "type": "command",
          "command": "state full"
        }
      ],
      "role": "observe",
      "expect": {
        "equals": {
          "cash": 50000,
          "activeContractCount": 2
        }
      }
    }
  ],
  "shots": [
    {
      "name": "overview",
      "yaw": 0,
      "pitch": 45
    },
    {
      "name": "contracts-available",
      "yaw": 90,
      "pitch": 10
    },
    {
      "name": "active-contracts",
      "yaw": 45,
      "pitch": 30
    },
    {
      "name": "finances-impact",
      "yaw": 0,
      "pitch": 80
    }
  ]
}
