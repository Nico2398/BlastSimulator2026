{
  "name": "crew-fleet-panels-visual",
  "description": "Redesign P6: Operations panel's unclaimed-work line, and (once wired in P6-cleanup) the Crew and Fleet panels themselves.",
  "shots": [
    { "name": "overview", "yaw": 0, "pitch": 45, "target": [5, 5], "distance": 15 }
  ],
  "steps": [
    {
      "command": "new_game seed:42 size:32",
      "timeout": 30,
      "description": "Setup: hire a driller and a driver so the crew roster is non-empty, buy a hauler and a drill rig (unlicensed) for the fleet.",
      "interaction": [
        { "type": "command", "command": "new_game seed:42 size:32" },
        { "type": "command", "command": "employee hire role:driller" },
        { "type": "command", "command": "employee hire role:driver" },
        { "type": "command", "command": "vehicle buy debris_hauler tier:1" },
        { "type": "command", "command": "vehicle buy drill_rig tier:1" }
      ]
    },
    {
      "command": "state",
      "timeout": 30,
      "description": "Operations panel: real logistics rows plus the new single 'Unclaimed Work' line (count from state.pendingActions, not one row repeated per eligible employee — that per-employee repeat is exactly what #39 removed).",
      "interaction": [
        { "type": "command", "command": "state" },
        { "type": "clickSelector", "selector": "#bs-toolbar [data-panel=\"ops\"]" },
        { "type": "wait", "durationMs": 300 },
        { "type": "screenshot" }
      ]
    },
    {
      "command": "state",
      "timeout": 30,
      "description": "Crew panel wired into UIManager (P6-cleanup): roster cards render with the driller and driver hired in step 0, under the ui.crew.* namespace.",
      "interaction": [
        { "type": "command", "command": "state" },
        { "type": "clickSelector", "selector": "#bs-toolbar [data-panel=\"employees\"]" },
        { "type": "wait", "durationMs": 300 },
        { "type": "screenshot" }
      ]
    },
    {
      "command": "state",
      "timeout": 30,
      "description": "Fleet panel wired into UIManager (P6-cleanup): vehicle cards render with the hauler and drill rig bought in step 0, under the ui.fleet.* namespace.",
      "interaction": [
        { "type": "command", "command": "state" },
        { "type": "clickSelector", "selector": "#bs-toolbar [data-panel=\"vehicles\"]" },
        { "type": "wait", "durationMs": 300 },
        { "type": "screenshot" }
      ]
    }
  ]
}
