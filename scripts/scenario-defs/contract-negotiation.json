{
  "name": "contract-negotiation",
  "description": "Negotiate contracts repeatedly to observe both improved and worsened pricing outcomes -- the file's original version never called `contract negotiate` at all, only accept/deliver; rewritten to exercise the actual negotiation mechanic (issue #479 follow-up).",
  "steps": [
    {
      "command": "campaign start level:dusty_hollow",
      "interaction": [
        {
          "type": "command",
          "command": "campaign start level:dusty_hollow"
        }
      ],
      "role": "setup",
      "expect": {
        "equals": {
          "cash": 50000
        }
      }
    },
    {
      "command": "time resume",
      "interaction": [
        {
          "type": "command",
          "command": "time resume"
        }
      ],
      "role": "setup"
    },
    {
      "command": "contract list",
      "interaction": [
        {
          "type": "command",
          "command": "contract list"
        }
      ],
      "role": "observe"
    },
    {
      "command": "contract negotiate type:supply material:dirtite",
      "description": "First negotiation attempt (round 1). No state field exists for a contract's own terms (price/deadline/penalty) -- there is nothing to negotiate over but the contracts a fresh dusty_hollow start already offers, so the target is fixed at the supply/dirtite offer throughout this file (confirmed id 1 in a fresh dusty_hollow start, issue #635) rather than reading it back from `contract list`'s text output, which expect can't do.",
      "interaction": [
        {
          "type": "clickSelector",
          "selector": "#bs-toolbar [data-panel=\"contracts\"]"
        },
        {
          "type": "waitForSelector",
          "selector": "#bs-contract-panel [data-action=\"negotiate\"]"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-contract-panel [data-action=\"negotiate\"]"
        }
      ],
      "role": "player",
      "expect": {
        "changedBy": {
          "cash": 0
        },
        "note": "negotiating costs nothing and moves no count -- the only real effect is on the contract's own terms, which no state field exposes. Confirmed via direct trace (seed:42, dusty_hollow): this first attempt genuinely FAILS (deadline worsened by 11%)."
      }
    },
    {
      "command": "tick 5",
      "description": "A tick between negotiation attempts, not just a pause -- contract negotiate's RNG is freshly seeded from state.seed + state.tickCount each call (economy.ts), so two negotiate calls at the same tick roll identically. This is what lets round 2 below land a genuinely different outcome.",
      "interaction": [
        {
          "type": "command",
          "command": "tick 5"
        }
      ],
      "role": "setup",
      "expect": {
        "changedBy": {
          "cash": 0
        }
      }
    },
    {
      "command": "contract negotiate type:supply material:dirtite",
      "description": "Second negotiation attempt (round 2) on the same still-available contract -- this is the file's own premise made real: repeated negotiation produces both outcomes.",
      "interaction": [
        {
          "type": "clickSelector",
          "selector": "#bs-contract-panel [data-action=\"negotiate\"]"
        }
      ],
      "role": "player",
      "expect": {
        "changedBy": {
          "cash": 0
        },
        "note": "confirmed via direct trace: this second attempt genuinely SUCCEEDS (deadline improved by 8%) -- opposite of round 1, proving the file's own claim (\"observe both improved and worsened pricing outcomes\") for real rather than asserting it never happened."
      }
    },
    {
      "command": "contract accept type:supply material:dirtite",
      "interaction": [
        {
          "type": "waitForSelector",
          "selector": "#bs-contract-panel [data-contract-type=\"supply\"][data-contract-material=\"dirtite\"] .bs-contract-accept"
        },
        {
          "type": "clickSelector",
          "selector": "#bs-contract-panel [data-contract-type=\"supply\"][data-contract-material=\"dirtite\"] .bs-contract-accept"
        }
      ],
      "role": "player",
      "expect": {
        "equals": {
          "activeContractCount": 1
        },
        "changedBy": {
          "cash": 0
        }
      },
      "description": "#513/#581/#635: the accept click is scoped to `[data-contract-type=\"supply\"][data-contract-material=\"dirtite\"]`, the contract this step's `command` names. Unscoped, `.bs-contract-accept` always resolves to whichever offered card is first in the DOM, so command mode accepted the supply/dirtite contract literally while interaction mode accepted whatever led the list -- two channels driving different contracts while both reported pass, because nothing asserted here can tell them apart (`activeContractCount` counts accepts, not identities). `level1-win-efficient` is what that costs once a scoped assertion is added on top: its guard failed with its control absent from the DOM entirely, red on `main`, cause pointing nowhere near the accept step. ContractsPanel sets `data-contract-type`/`data-contract-material` on offered cards (`makeOfferedCard`), so scoping removes the DOM-order coupling rather than baking around it -- and, unlike the numeric id it replaces (#635), survives the offer pool rotating past it."
    },
    {
      "command": "tick 10",
      "interaction": [
        {
          "type": "command",
          "command": "tick 10"
        }
      ],
      "role": "setup",
      "expect": {
        "equals": {
          "activeContractCount": 1
        },
        "changedBy": {
          "cash": 0
        }
      }
    },
    {
      "command": "contract deliver type:supply material:dirtite amount:50",
      "interaction": [
        {
          "type": "command",
          "command": "contract deliver type:supply material:dirtite amount:50"
        }
      ],
      "description": "role: guard (issue #515): both the amount input and DELIVER button are genuinely disabled here \u2014 `amountInput.disabled = maxDeliverable <= 0` and `deliverBtn.disabled = maxDeliverable <= 0` (ContractsPanel.ts) \u2014 this file's scope is negotiation and accept/decline, not the full haul-to-storage pipeline, so nothing is ever in storage. A real click cannot land on either control, so this proves the same refusal `expect.blocked` already asserted (now scoped to `[data-contract-type=\"supply\"][data-contract-material=\"dirtite\"]`, issue #513/#635) rather than performing one.",
      "expect": {
        "equals": {
          "activeContractCount": 1
        },
        "changedBy": {
          "cash": 0
        },
        "usable": "#bs-contract-panel [data-action=\"goto-ops\"]",
        "blocked": "#bs-contract-panel [data-contract-type=\"supply\"][data-contract-material=\"dirtite\"] .bs-contract-deliver",
        "note": "the contract stays active/undelivered -- this file's scope is negotiation and accept/decline, not the full haul-to-storage pipeline"
      },
      "role": "guard",
      "commandOutcome": "refused"
    },
    {
      "command": "finances",
      "interaction": [
        {
          "type": "command",
          "command": "finances"
        }
      ],
      "role": "observe"
    },
    {
      "command": "state full",
      "interaction": [
        {
          "type": "command",
          "command": "state full"
        }
      ],
      "role": "observe",
      "expect": {
        "equals": {
          "activeContractCount": 1
        },
        "changedBy": {
          "cash": 0
        }
      }
    }
  ]
}
