# Skip Puppeteer's automatic Chrome download — browser is provided by the environment
# Visual tests use the system-installed Chromium
# (Puppeteer skip-download is configured in package.json under "puppeteer.skipDownload")
