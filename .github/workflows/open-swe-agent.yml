name: Open SWE Agent

on:
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Issue or PR number'
        required: true
        type: string
      comment_body:
        description: 'Triggering comment or action description'
        required: false
        type: string
  issue_comment:
    types: [created]

jobs:
  run-open-swe:
    name: Execute Open SWE
    # Trigger when:
    #  - manually dispatched from the Actions tab (workflow_dispatch)
    #  - a comment on an issue or PR contains '@openswe' (repo owner only)
    if: "github.event_name == 'workflow_dispatch' || (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@openswe') && github.event.comment.author_association == 'OWNER')"
    runs-on: ubuntu-latest

    # These write permissions are required:
    #  - issues:write       → post the 👀 reaction
    #  - contents:write     → push the agent's branch
    #  - pull-requests:write → open the PR
    permissions:
      issues: write
      contents: write
      pull-requests: write

    steps:
      # ── 1. Acknowledge ──────────────────────────────────────────────────────
      - name: Acknowledge Request
        uses: actions/github-script@v7
        with:
          script: |
            const isDispatch = context.eventName === 'workflow_dispatch';
            let issueNumber;
            if (isDispatch) {
              issueNumber = parseInt(context.payload.inputs.issue_number.replace(/\D/g, ''), 10);
              if (!issueNumber) core.setFailed('issue_number must be a valid integer'); return;
            } else {
              issueNumber = context.payload.comment
                ? context.issue.number
                : (context.payload.issue?.number ?? context.payload.pull_request?.number);
            }

            if (!isDispatch && context.payload.comment) {
              // React to the triggering comment
              await github.rest.reactions.createForIssueComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: context.payload.comment.id,
                content: 'eyes'
              });
            } else {
              // Post a comment to acknowledge assignment, review request, or dispatch
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                body: '👀 Open SWE Agent picking this up…'
              });
            }

      # ── 2. Checkout this repository ─────────────────────────────────────────
      - name: Checkout BlastSimulator2026
        uses: actions/checkout@v4

      # ── 3. Checkout Open SWE ────────────────────────────────────────────────
      - name: Checkout Open SWE Repository
        uses: actions/checkout@v4
        with:
          repository: langchain-ai/open-swe
          path: open-swe

      # ── 4. Python + uv ──────────────────────────────────────────────────────
      # langgraph.json specifies python_version 3.12
      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Open SWE Dependencies
        working-directory: ./open-swe
        run: |
          pip install uv
          uv sync --all-extras

      # ── 5. Patch make_model() for DeepSeek ──────────────────────────────────
      # Open-SWE's make_model() in agent/utils/model.py always overrides base_url
      # to wss://api.openai.com/v1 (the OpenAI Responses WebSocket) for any model
      # with the "openai:" prefix. That breaks DeepSeek's OpenAI-compatible endpoint.
      # This one-line patch makes it skip the override when OPENAI_BASE_URL is set.
      - name: Patch model.py for OpenAI-compatible providers
        working-directory: ./open-swe
        run: |
          python - <<'EOF'
          path = 'agent/utils/model.py'
          src = open(path).read()
          if 'import os\n' not in src:
              src = 'import os\n' + src
          src = src.replace(
              'if model_id.startswith("openai:"):',
              'if model_id.startswith("openai:") and not os.environ.get("OPENAI_BASE_URL"):'
          )
          open(path, 'w').write(src)
          print("Patch applied to", path)
          EOF

      # ── 5b. Patch auth.py to fall back to GITHUB_TOKEN ──────────────────────
      # resolve_github_token() looks up github_login → email → LangSmith OAuth.
      # We don't use LangSmith OAuth here — GITHUB_TOKEN from the runner is
      # sufficient. Insert a short-circuit: if GITHUB_TOKEN is in env, use it
      # directly and skip the email-mapping lookup entirely.
      - name: Patch auth.py to use GITHUB_TOKEN
        working-directory: ./open-swe
        run: |
          python - <<'EOF'
          import re, sys
          path = 'agent/utils/auth.py'
          src = open(path).read()
          # Ensure 'import os' is present (handle any whitespace variation)
          if not re.search(r'^import os\b', src, re.MULTILINE):
              src = 'import os\n' + src
          old = '            github_login = configurable.get("github_login")'
          new = (
              '            env_token = os.environ.get("GITHUB_TOKEN", "")\n'
              '            if env_token:\n'
              '                encrypted = await persist_encrypted_github_token(thread_id, env_token)\n'
              '                return env_token, encrypted, None\n'
              '            github_login = configurable.get("github_login")'
          )
          if old not in src:
              print("ERROR: anchor not found in auth.py — cannot apply patch", file=sys.stderr)
              sys.exit(1)
          src = src.replace(old, new, 1)
          open(path, 'w').write(src)
          print("Patch applied to", path)
          EOF

      # ── 5c. Patch langchain for DeepSeek reasoning_content ──────────────────
      # DeepSeek thinking models (e.g. deepseek-v4-flash) return `reasoning_content`
      # in assistant messages. LangChain drops this field when serializing message
      # history, causing DeepSeek to reject multi-turn conversations with:
      #   "The reasoning_content in the thinking mode must be passed back to the API."
      # Patch 1: preserve reasoning_content (API response → AIMessage.additional_kwargs)
      # Patch 2: re-inject reasoning_content (AIMessage.additional_kwargs → API request)
      # Patch 3: same fix applied to langchain_openai if it imports the fn locally
      - name: Patch langchain for DeepSeek reasoning_content
        working-directory: ./open-swe
        run: |
          uv run python - <<'EOF'
          import inspect, re, sys
          print("=== DeepSeek reasoning_content patch ===")

          import langchain_core.messages.utils as mu
          path = inspect.getfile(mu)
          src = open(path).read()
          print(f"File: {path}")
          changed = False

          # --- Patch 1: convert_dict_to_message ---
          # Store reasoning_content into AIMessage.additional_kwargs when parsing response
          SENTINEL1 = "# deepseek-reasoning-p1"
          if SENTINEL1 in src:
              print("Patch 1: already applied")
          else:
              m = re.search(r'([ \t]+)(additional_kwargs\s*:\s*Dict\s*=\s*\{\})', src)
              if m:
                  indent = m.group(1)
                  anchor = m.group(0)
                  inject = (
                      f"\n{indent}{SENTINEL1}\n"
                      f"{indent}if _dict.get(\"reasoning_content\"):\n"
                      f"{indent}    additional_kwargs[\"reasoning_content\"] = _dict[\"reasoning_content\"]\n"
                  )
                  src = src.replace(anchor, anchor + inject, 1)
                  changed = True
                  print("Patch 1: applied — reasoning_content preserved in convert_dict_to_message")
              else:
                  print("Patch 1: WARNING — anchor not found in", path, file=sys.stderr)

          # --- Patch 2: convert_message_to_dict ---
          # Re-include reasoning_content from AIMessage.additional_kwargs in API request dict
          SENTINEL2 = "# deepseek-reasoning-p2"
          if SENTINEL2 in src:
              print("Patch 2: already applied")
          else:
              m2 = re.search(r'([ \t]+)(if "function_call" in message\.additional_kwargs:)', src)
              if m2:
                  indent2 = m2.group(1)
                  anchor2 = m2.group(0)
                  inject2 = (
                      f"{indent2}{SENTINEL2}\n"
                      f"{indent2}if \"reasoning_content\" in message.additional_kwargs:\n"
                      f"{indent2}    message_dict[\"reasoning_content\"] = message.additional_kwargs[\"reasoning_content\"]\n"
                      f"{anchor2}"
                  )
                  src = src.replace(anchor2, inject2, 1)
                  changed = True
                  print("Patch 2: applied — reasoning_content re-injected in convert_message_to_dict")
              else:
                  print("Patch 2: WARNING — anchor not found in", path, file=sys.stderr)

          if changed:
              open(path, 'w').write(src)
              print(f"Written: {path}")

          # --- Patch 3: langchain_openai (may import convert_message_to_dict locally) ---
          try:
              import langchain_openai.chat_models.base as cmb
              cmb_path = inspect.getfile(cmb)
              cmb_src = open(cmb_path).read()
              SENTINEL3 = "# deepseek-reasoning-p3"
              if SENTINEL3 in cmb_src:
                  print("Patch 3: already applied")
              else:
                  m3 = re.search(r'([ \t]+)(if "function_call" in message\.additional_kwargs:)', cmb_src)
                  if m3:
                      indent3 = m3.group(1)
                      anchor3 = m3.group(0)
                      inject3 = (
                          f"{indent3}{SENTINEL3}\n"
                          f"{indent3}if \"reasoning_content\" in message.additional_kwargs:\n"
                          f"{indent3}    message_dict[\"reasoning_content\"] = message.additional_kwargs[\"reasoning_content\"]\n"
                          f"{anchor3}"
                      )
                      cmb_src = cmb_src.replace(anchor3, inject3, 1)
                      open(cmb_path, 'w').write(cmb_src)
                      print(f"Patch 3: applied to langchain_openai ({cmb_path})")
                  else:
                      print(f"Patch 3: anchor not found in langchain_openai (likely not needed)")
          except Exception as e:
              print(f"Patch 3: skipped ({e})")

          print("=== Patch complete ===")
          EOF

      # ── 6. Inject custom tools and agent context ───────────────────────────
      # Copies every *.py file from .openswe/tools/ into open-swe's tools package,
      # then auto-discovers all public (non-underscore) top-level functions in those
      # files and patches agent/server.py to import and register them.
      # Also copies AGENTS.md (the agent's system prompt) into the open-swe directory
      # so it is available at the path referenced by DEFAULT_PROMPT_PATH.
      - name: Inject custom tools and agent context
        run: |
          # Copy every tool file and AGENTS.md
          cp $GITHUB_WORKSPACE/.openswe/tools/*.py ./open-swe/agent/tools/
          cp $GITHUB_WORKSPACE/.openswe/AGENTS.md ./open-swe/AGENTS.md

          python - <<'EOF'
          import ast, glob, os

          tools_src = os.environ["GITHUB_WORKSPACE"] + "/.openswe/tools"
          server_path = "open-swe/agent/server.py"

          # Discover public top-level functions per tool module
          module_fns: dict[str, list[str]] = {}
          for fpath in sorted(glob.glob(f"{tools_src}/*.py")):
              mod = os.path.splitext(os.path.basename(fpath))[0]
              top_fns = [
                  n.name for n in ast.parse(open(fpath).read()).body
                  if isinstance(n, ast.FunctionDef) and not n.name.startswith("_")
              ]
              if top_fns:
                  module_fns[mod] = top_fns

          if not module_fns:
              print("No public tool functions found — skipping server.py patch")
              raise SystemExit(0)

          # Build import block
          import_block = ""
          for mod, fns in module_fns.items():
              import_block += f"from .tools.{mod} import (\n"
              for fn in fns:
                  import_block += f"    {fn},\n"
              import_block += ")\n"

          # Flat list of all tool function names
          tool_entries = [fn for fns in module_fns.values() for fn in fns]

          # Patch server.py
          src = open(server_path).read()

          # 1. Inject imports before the existing '.tools' package import
          anchor_import = "from .tools import ("
          src = src.replace(anchor_import, import_block + anchor_import, 1)

          # 2. Append tool entries just before the closing ']' of the tools list.
          #    Parse the *original* src (before import insertion) to find the line.
          orig_src = open(server_path).read()
          tree = ast.parse(orig_src)
          insert_line = None
          for node in ast.walk(tree):
              if (
                  isinstance(node, ast.Assign)
                  and any(isinstance(t, ast.Name) and t.id == "tools" for t in node.targets)
                  and isinstance(node.value, ast.List)
              ):
                  insert_line = node.value.end_lineno  # 1-based line of closing ']'
                  break

          if insert_line:
              lines = src.splitlines(keepends=True)
              # After import insertion the line numbers shifted by the number of
              # import lines added, so recalculate from the modified src.
              tree2 = ast.parse(src)
              for node in ast.walk(tree2):
                  if (
                      isinstance(node, ast.Assign)
                      and any(isinstance(t, ast.Name) and t.id == "tools" for t in node.targets)
                      and isinstance(node.value, ast.List)
                  ):
                      insert_line = node.value.end_lineno
                      break
              lines = src.splitlines(keepends=True)
              indent = "            "
              new_entries = "".join(f"{indent}{fn},\n" for fn in tool_entries)
              lines.insert(insert_line - 1, new_entries)
              src = "".join(lines)
          else:
              print("WARNING: could not locate tools list in server.py via AST")

          open(server_path, "w").write(src)
          all_fns = ", ".join(tool_entries)
          print(f"Injected tools: {all_fns}")
          EOF

      # ── 7. Configure git identity ────────────────────────────────────────────
      # The agent's commit_and_open_pr tool needs git credentials.
      # GITHUB_TOKEN (auto-provisioned) is used for HTTPS pushes via the URL rewrite.
      - name: Configure git credentials
        run: |
          git config --global user.email "open-swe-bot@github.com"
          git config --global user.name "Open SWE Bot"
          git config --global \
            url."https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/".insteadOf \
            "https://github.com/"

      # ── 8. Resolve issue number ──────────────────────────────────────────────
      # Only the number is needed here. The agent fetches full context itself
      # via the github_get_issue / github_get_pr tools injected in step 6.
      - name: Resolve Issue Number
        id: issue
        uses: actions/github-script@v7
        with:
          script: |
            const isDispatch = context.eventName === 'workflow_dispatch';
            let issueNumber;
            if (isDispatch) {
              issueNumber = parseInt(context.payload.inputs.issue_number.replace(/\D/g, ''), 10);
            } else {
              issueNumber = context.payload.issue?.number ?? context.payload.pull_request?.number;
            }
            if (!issueNumber || isNaN(issueNumber)) {
              core.setFailed(`Could not resolve a valid issue/PR number (got: ${issueNumber})`);
              return;
            }
            core.setOutput('number', String(issueNumber));

      # ── 8b. Generate ephemeral Fernet encryption key ────────────────────────
      # open-swe's encryption.py requires TOKEN_ENCRYPTION_KEY to be a valid
      # Fernet key. It is only used within this single run (ephemeral).
      - name: Generate encryption key
        run: |
          python - <<'PY'
          import base64
          import os

          key = base64.urlsafe_b64encode(os.urandom(32)).decode()
          with open(os.environ["GITHUB_ENV"], "a") as f:
              f.write(f"TOKEN_ENCRYPTION_KEY={key}\n")
          print("TOKEN_ENCRYPTION_KEY generated.")
          PY

      # ── 9. Start server + run agent ──────────────────────────────────────────
      #
      # MODEL
      #   LLM_MODEL_ID=openai:deepseek-v4-flash
      #     "openai:" prefix → uses langchain-openai (ChatOpenAI) as the provider.
      #     OPENAI_BASE_URL  → points ChatOpenAI at DeepSeek's OpenAI-compatible endpoint.
      #     OPENAI_API_KEY   → your DeepSeek API key (stored as DEEPSEEK_API_KEY secret).
      #
      # SANDBOX
      #   SANDBOX_TYPE=local runs every shell command directly on this Actions runner.
      #   The multi-agent "task" tool (subagent spawning) works in local mode —
      #   subagents share the same runner environment.
      #
      #   For full isolated cloud sandboxes per subagent (true multi-agent parallelism),
      #   add these secrets and switch SANDBOX_TYPE to "langsmith":
      #     LANGSMITH_API_KEY_PROD    → your LangSmith API key
      #     LANGSMITH_TENANT_ID_PROD  → LangSmith tenant UUID
      #     DEFAULT_SANDBOX_SNAPSHOT_ID → LangSmith sandbox snapshot UUID
      #
      # GITHUB CREDENTIALS
      #   The built-in GITHUB_TOKEN is used here for git push and PR creation.
      #   For full per-user identity (commits attributed to the triggering user),
      #   set up a GitHub App (see INSTALLATION.md) and add:
      #     GITHUB_APP_ID, GITHUB_APP_PRIVATE_KEY, GITHUB_APP_INSTALLATION_ID
      #
      - name: Run Open SWE Agent
        working-directory: ./open-swe
        env:
          # DeepSeek via OpenAI-compatible API
          OPENAI_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          OPENAI_BASE_URL: "https://api.deepseek.com/v1"
          LLM_MODEL_ID: "openai:deepseek-v4-flash"

          # Sandbox (local = run directly on this Actions runner)
          SANDBOX_TYPE: "local"

          # Target repository
          DEFAULT_REPO_OWNER: ${{ github.repository_owner }}
          DEFAULT_REPO_NAME: ${{ github.event.repository.name }}

          # GitHub token for git push + PR creation
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

          # Fernet key for token encryption inside open-swe (generated in step 8b)
          TOKEN_ENCRYPTION_KEY: ${{ env.TOKEN_ENCRYPTION_KEY }}

          # LangSmith tracing — set LANGSMITH_API_KEY in repo secrets to enable
          LANGCHAIN_TRACING_V2: ${{ secrets.LANGSMITH_API_KEY != '' && 'true' || 'false' }}
          LANGSMITH_API_KEY: ${{ secrets.LANGSMITH_API_KEY }}
          LANGCHAIN_PROJECT: "BlastSimulator2026-openswe"

          # Custom project instructions — injected as the default prompt (copied in step 6)
          DEFAULT_PROMPT_PATH: ${{ github.workspace }}/open-swe/AGENTS.md

          # Issue/PR context — only the number is passed; the agent fetches the
          # rest via the github_get_issue / github_get_pr tools.
          ISSUE_NUMBER: ${{ steps.issue.outputs.number }}
          COMMENT_BODY: "${{ github.event.inputs.comment_body || github.event.comment.body || format('@openswe triggered manually on #{0}', github.event.inputs.issue_number) }}"

        run: |
          echo "=== Open SWE Agent run ==="
          echo "Issue   : $ISSUE_NUMBER"
          echo "Repo    : $DEFAULT_REPO_OWNER/$DEFAULT_REPO_NAME"
          echo "Model   : $LLM_MODEL_ID"
          echo "Sandbox : $SANDBOX_TYPE"
          echo "Comment : $COMMENT_BODY"
          echo "==========================="

          # Start the LangGraph in-memory server in the background.
          # All env vars above are inherited by the background process.
          nohup uv run langgraph dev --port 2024 > /tmp/langgraph.log 2>&1 &

          # Wait up to 90 s for the health endpoint to respond.
          echo "Waiting for LangGraph server..."
          for i in $(seq 1 90); do
            if curl -sf http://localhost:2024/ok > /dev/null 2>&1; then
              echo "Server ready after ${i}s"; break
            fi
            sleep 1
          done
          curl -sf http://localhost:2024/ok || { echo "Server failed to start:"; cat /tmp/langgraph.log; exit 1; }

          # Invoke the agent via the LangGraph Python SDK.
          uv run python - <<'PYEOF'
          import asyncio
          import os
          import sys

          import uuid

          from langgraph_sdk import get_client

          async def main():
              client = get_client(url="http://localhost:2024")

              repo_owner   = os.environ["DEFAULT_REPO_OWNER"]
              repo_name    = os.environ["DEFAULT_REPO_NAME"]
              comment_body = os.environ["COMMENT_BODY"]

              raw_issue = os.environ.get("ISSUE_NUMBER", "")
              try:
                  issue_number = int(raw_issue)
              except ValueError:
                  print(f"ERROR: ISSUE_NUMBER is not a valid integer: {raw_issue!r}", file=sys.stderr)
                  sys.exit(1)

              issue_number_str = str(issue_number)

              # Minimal context: just the reference. The agent fetches full
              # details via github_get_issue() / github_get_pr() tools.
              task = (
                  f"Task: resolve GitHub issue #{issue_number_str} in {repo_owner}/{repo_name}.\n\n"
                  f"## Step 1 — fetch issue context (always required)\n"
                  f"  github_get_issue({issue_number_str})\n"
                  f"  github_list_issue_comments({issue_number_str})\n\n"
                  f"## Step 2 — fetch PR context (when the task involves a pull request)\n"
                  f"If the issue IS a PR, or the user instruction mentions PR review feedback:\n"
                  f"  github_get_pr({issue_number_str})\n"
                  f"  github_get_pr_files({issue_number_str})\n"
                  f"  github_get_pr_reviews({issue_number_str})\n"
                  f"  github_get_pr_review_comments({issue_number_str})  ← inline code comments\n\n"
                  f"Do not write any code until you have read all relevant context above.\n\n"
                  f"## User instruction\n{comment_body}"
              )

              # Deterministic UUID thread ID so follow-up comments route to the same run.
              # LangGraph requires thread IDs to be UUIDs — derive one via uuid5.
              # Keep the existing DNS namespace so thread IDs stay stable across follow-up comments.
              _ns = uuid.NAMESPACE_DNS
              thread_id = str(uuid.uuid5(_ns, f"github-{repo_owner}-{repo_name}-{issue_number_str}"))

              print(f"Starting agent run for {repo_owner}/{repo_name}#{issue_number_str}...")
              sys.stdout.flush()

              run = await client.runs.create(
                  thread_id,
                  "agent",
                  input={"messages": [{"role": "user", "content": task}]},
                  config={"configurable": {
                      "repo": {"owner": repo_owner, "name": repo_name},
                      "source": "github",
                      "__is_for_execution__": True,
                  }},
                  if_not_exists="create",
              )

              print(f"Run ID: {run['run_id']}")
              sys.stdout.flush()

              # Poll until the run reaches a terminal state.
              while True:
                  run = await client.runs.get(thread_id, run["run_id"])
                  status = run["status"]
                  print(f"Status: {status}")
                  sys.stdout.flush()
                  if status in ("success", "error", "interrupted"):
                      break
                  await asyncio.sleep(10)

              if run["status"] != "success":
                  # Print any structured error details from the run object
                  run_error = run.get("error") or run.get("kwargs", {}).get("error")
                  if run_error:
                      print(f"Run error: {run_error}", file=sys.stderr)
                  # Tail the server log to surface the real exception inline
                  print("\n--- LangGraph server log (last 60 lines) ---", file=sys.stderr)
                  try:
                      with open("/tmp/langgraph.log") as lf:
                          lines = lf.readlines()
                          sys.stderr.write("".join(lines[-60:]))
                  except Exception as le:
                      print(f"(could not read langgraph.log: {le})", file=sys.stderr)
                  print("--- end of server log ---", file=sys.stderr)
                  sys.stderr.flush()
                  print("Agent run did not succeed.", file=sys.stderr)
                  sys.exit(1)

              print("Agent completed successfully.")

          asyncio.run(main())
          PYEOF

      # ── 10. Debug logs on failure ────────────────────────────────────────────
      - name: Dump LangGraph server log
        if: failure()
        run: |
          echo "=== LangGraph server log ==="
          cat /tmp/langgraph.log || echo "(log not found)"
          echo "=== end of server log ==="
